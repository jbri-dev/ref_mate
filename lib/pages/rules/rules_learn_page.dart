import 'package:flutter/material.dart';

class RulesLearnPage extends StatelessWidget {
  const RulesLearnPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Regelfragen – Lernen (Random)'));
  }
}
