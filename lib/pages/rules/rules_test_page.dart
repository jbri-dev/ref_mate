import 'package:flutter/material.dart';

class RulesTestPage extends StatelessWidget {
  const RulesTestPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Regelfragen – Testmodus'));
  }
}
